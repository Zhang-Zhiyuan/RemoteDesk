# 1.0.13

修复 Windows 发送文件夹时，文件修改时间超出 ZIP 支持范围导致整个打包失败的问题。现在与 Linux 一致：仅将压缩包内不支持的时间限制到 1980–2107 年，原文件时间和内容不变，正常时间照常保留。

Android 与 Linux 功能不变，安装包版本统一更新。APK 沿用原签名。没有调整画质、码率、录屏权限或公网路由。

仍为预发布：本轮没有手机真机，Linux → Windows 的实际输入验收尚未完成。Windows EXE 未做 Authenticode 签名；其他系统权限限制见[说明](permissions.md)。
