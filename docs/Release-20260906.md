# 2026-09-06 发布整理

此文保留首次切换与清理的历史记录；之后的优化和标准重新打包见[后续复查](Recheck-20260906.md)，当前包以 `artifacts/RemoteDesk-release-manifest.json` 为准。

## 本次 Windows 修复

- 设置写入失败会返回明确结果，主界面持续显示“设置未保存”和“重试保存”；修改备注、删除设备、中继配置不会再把未保存的操作提示为成功。
- 保留失败后的内存更改和原配置文件，恢复可写状态后能够重试。退出前仍未保存的内存更改会丢失。
- 中继在线列表标注本机并禁止自连接；按钮和连接入口都校验设备 ID，兼容 GUID 格式差异。同名远端及已被使用的远端不被误拦截，后来认证成功者接管的行为保持不变。

## 验证记录

- Windows 最后一次全量回归：1549 通过、0 失败、11 项硬件/真机测试跳过，共 1560 项；本次新增 18 项回归。
- 新保存提示条已检查 360 px 和 860 px 布局，以及真实重试按钮回调。
- 切换与整理时补跑设置、中继设备选择、提示条和发布脚本专项：57 项通过、0 失败。
- 此前同轮 Linux/Python：214 项通过；隔离 Linux 桌面的收帧、输入、剪贴板、文件回传、认证、会话接管等 9 个实际场景通过。
- 此前同轮 Android：307 项单元测试通过；APK 构建和 lint 通过，遗留 1 条文案国际化警告。此次 Windows 修复未修改 Linux/Android 业务源码。
- 新 Windows 包已替换本地运行版本，以托盘模式重新启动；56565 监听和未认证的 `RDK1` 握手通过，原有 3 个保存设备、中继设置和默认被控保持不变。

本记录不替代公网中继部署、实体 Android 权限/录屏、多屏热插拔、高权限输入、硬件 4K60 或长稳验收。Android/Linux 客户端的中继入口尚未实现。

## 包标识与仓库整理

- Windows 构建时间标记：`20260905171802`（UTC）。
- Windows EXE SHA-256：`3456345d3067fafe279f5c4b0cd27318807c91df713960901939f623f0939fd9`。
- Windows ZIP SHA-256：`26bc8963c127498486df1e37c2b92e50bc96d9c0362c942ee7c61d6870238e30`。
- Linux/Android 包保留 `20260904081545` 原构建来源，未将重新验证描述为重新构建。
- 正式包保留于本地 `artifacts`；15 个可重建缓存目录（454.41 MiB）、59 项临时排查文件和原始报告（343.95 MiB）转移到仓库外备份，旧 Windows 包另外备份。未永久删除，不能据此宣称磁盘可用空间增加。
- 忽略规则已准备好；历史已跟踪的安装包和机器相关验收输出仍待移出 Git 索引。8 月 18 日遗留的空 `.git/index.lock` 阻止索引更新，当前执行环境也拒绝移动该锁，故本轮尚未提交或上传。
- 当前没有配置 Git 远端；Git 对象约 18.3 GiB，旧历史包含大体积安装包。已提交的旧历史不因新增忽略规则而消失；历史清理须先备份并确认，不能把工作区清理描述为历史瘦身。原有 agent 工作树及其独有提交保持原状。
