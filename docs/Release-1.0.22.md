# 1.0.22

- 三端远控窗口增加“新版放大”实验开关，默认关闭，关闭即恢复原版。
- Windows 可选 NIS 边缘自适应放大或双三次＋防光晕。右键开关选择算法；NIS 限 1～2 倍，超范围或不兼容时自动回退，底部显示实际算法。
- 安卓、Linux 提供 GPU 双三次＋防光晕；保留硬解和原有缩放、触控坐标。

只改变接收端显示，不改变传输分辨率和协议。本次功能不新增权限，不要求重配中继或修改设备密钥。

放大功能仍是实验项：不能还原已丢失的笔画，不保证每台显卡都更快。手机真机和不同 Linux 显卡仍需进一步验证。
