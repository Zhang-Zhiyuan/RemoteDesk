<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No Internet, capture, accessibility, storage or administrator permission. -->
    <application android:label="RemoteDesk Codec Test" android:allowBackup="false"
        android:supportsRtl="true" android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity android:name="com.remotedesk.agent.CodecProbeActivity" android:exported="true" />
        <activity android:name="com.remotedesk.agent.InteractionProbeActivity" android:exported="true" />
    </application>
</manifest>
